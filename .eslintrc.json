{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    // Disable specific rules or set their severity
    "no-console": "off",
    "react-hooks/exhaustive-deps": "warn"
  },
  "overrides": [
    {
      "files": ["**/*.ts", "**/*.tsx"],
      "rules": {
        "no-unused-vars": "off"
      }
    }
  ]
}
